services:
  - type: web
    name: credit-risk-api
    runtime: python
    buildCommand: pip install -r requirements.txt && pip install lightgbm==4.6.0
    startCommand: PYTHONPATH=. uvicorn api.app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
